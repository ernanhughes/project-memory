"""Persistent derived memory over project history (book Chapter 4).

Conventional RAG (``memory_baseline``) retrieves raw passages and lets a
reader reconstruct meaning per query. This package preserves AI-derived
interpretation — entities, relationships, claims, communities — as reusable
state, then queries raw and derived memory together with the original
sources retained as the evidentiary substrate.
"""
