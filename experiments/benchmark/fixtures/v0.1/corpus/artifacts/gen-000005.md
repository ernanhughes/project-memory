commit-112 — commit
Date: 2024-07-15

Move application writes to PostgreSQL on migration branch

On the migration branch, application writes change to PostgreSQL and are exercised in staging. Production still runs SQLite.
